package SeleniumInterview;

public class Note {
	
	/// why do we use maven?
	// we use maven management tool for dependencies and plugins, JRE files
	// plugin is genetated reports and loge the details
	// POM  stands for Page Object Madule
	// XML stands for open and close tag <Open tag> and <\close tag>
	
	// Implicit time global
	// Implicit time  for 30 second, the code will be excuate any second once the element is presented
	// Thread wiat time for 50 second, the code will be not  move to next line or excuate until 50 second
	// Thread wiat time is Java commond, not selenium
	
	
	// FluentWait attempting every 5 second till emelemnt is persent or go to the excptions
	
	
	// Select tag, we have select method, SelectbyVisibleText,Selectbyindex, and SelectbyValue
	//WebElement searchDropDown = driver.findElement(By.id("search"));
	
	//Explicit Wait command, wait until a certain condition occurs 
	//before proceeding with executing the
	
			//Select select = new Select(searchDropDown);
			//select.selectByIndex(3);
			//select.selectByVisibleText("Electronics");//selectByVisibleText
	
	

}
